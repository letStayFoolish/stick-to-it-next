import React from "react";
import ShoppingListLoading from "@/app/(private)/shopping-list/components/ShoppingListLoading";

const Loading = () => {
  return <ShoppingListLoading />;
};

export default Loading;
